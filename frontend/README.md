# Frontend

The active frontend lives in this directory and is deployed to Vercel for `https://hypd.games`.

## Local Development

```bash
npm install
npm run dev
```

The app expects:
- `NEXT_PUBLIC_API_URL`
- `NEXT_PUBLIC_SITE_URL`

Use `frontend/.env.example` as the template for local setup.
